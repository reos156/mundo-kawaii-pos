# Mundo Kawaii POS MVP — ODD Task Ledger

## Objective
Build a web POS for one physical store and one register, using Supabase and a Bun/Turborepo monorepo with React, TypeScript, Vite, TanStack Router, and Tailwind CSS. The MVP must keep sales, collections, reservations, inventory, and physical cash as distinct concepts.

## Problem
The store needs to manage physical products in whole units, several simultaneous pre-accounts, customer reservations, cash and manually verified bank transfers, inventory movements, cash sessions, consultations, administration, and permissions without counting pending activity as completed sales or cash.

## Why
The POS is a system of record for product availability and store money. Checkout and stock transitions must be atomic, authorized, auditable, and understandable to cashiers and administrators.

## Scope
### Included
- Single store and single register; Spanish (Colombia) user interface and COP amounts represented as whole pesos.
- Product catalog and integer inventory, including stock receipts, authorized adjustments, and movement history.
- Multiple independent, persistent pre-accounts.
- Customer reservations that hold available stock without creating a sale, collection, or cash movement.
- Cash checkout and manual bank-transfer verification.
- Cash sessions, opening float, cash movements, expected-vs-counted closing, and discrepancy visibility.
- Role-based administration and authorization; sales, collections, reservations, stock, and cash consultations.
- Internal non-fiscal receipt suitable for browser printing.
- Supabase Auth/Postgres, row-level security, database-enforced authorization, and transactional database operations for critical transitions.

### Excluded unless the user changes scope
- DIAN electronic invoicing or claims that the internal receipt is a fiscal invoice.
- Multiple stores/registers, offline operation, payment-provider integrations, and automatic bank verification.
- Supplier purchasing workflows, serial/lot tracking, fractional quantities, and dedicated thermal-printer integration.
- Split-tender checkout; the initial workflow accepts one payment method per checkout.
- Production deployment or use of production credentials.

## Constraints and accepted decisions
- Work from the requirements in the current user request only. Preserve pre-existing untracked planning artifacts; do not use them as product requirements or overwrite them.
- The internal receipt is non-fiscal; electronic invoicing is outside this MVP.
- A pre-account is not a sale or payment and does not hold stock.
- A customer reservation is separate from a pre-account and sale; it reduces available stock until converted, cancelled, or expired.
- A transfer awaiting manual verification holds checkout stock but is not a confirmed sale or cash movement. Approval confirms the payment and sale and consumes the held inventory; rejection releases the hold. The user selected this rule.
- Cash checkout confirms the cash payment and sale and records the stock and cash movements atomically.
- Initial roles are cashier and administrator/supervisor; privileged operations are checked server-side and audited.
- Product catalog changes, stock receipts, and stock adjustments are administrator-only; cashiers have read-only inventory access. This permission boundary was explicitly confirmed while resuming POS-04.
- Strict TDD is enabled by explicit user choice. TypeScript/UI tests use Vitest through root `bun run test`; SQL/RLS tests use pgTAP through Supabase CLI `supabase test db` after `supabase start`. Do not claim RED/GREEN evidence before observing it.
- This checkout initially had no app package or runner configuration; POS-02 established Vitest and the root `bun run test` command. Supabase CLI is not globally installed; Docker is available. Add the CLI as a local development tool and keep database tests local.
- Runner documentation consulted: [Supabase database testing](https://supabase.com/docs/guides/database/testing) and [Supabase local testing overview](https://supabase.com/docs/guides/local-development/testing/overview).
- The user explicitly authorized four ordered work-unit commits for POS-01 through POS-04, selected `stacked-to-main` for future PRs, and explicitly accepted `size:exception` for the POS-02, POS-03, and POS-04 PR slices. This authorizes only these four commits; it does not authorize push, PR creation, or merge. Keep the commits on `feat/planning` and preserve task boundaries.

## Acceptance criteria
1. Domain language explicitly distinguishes pre-accounts, reservations, checkout/payment verification, confirmed sales, inventory movements, and cash sessions.
2. User and system flows are documented in Mermaid, including cash checkout, pending/approved/rejected transfer, reservation, and cash close.
3. Supabase authorization cannot be bypassed by UI-only checks; privileged writes validate roles in the database.
4. Product quantities and stock are whole units, COP prices use integer pesos, and stock cannot become negative under concurrent operations.
5. Multiple pre-accounts can remain open independently; editing or abandoning one does not create sales, payments, inventory movements, or cash movements.
6. Active reservations and pending transfer checkouts reduce available stock without being reported as completed sales.
7. Transfer approval/rejection is authorized and audited; approval creates the sale and inventory movement, while rejection releases the held stock.
8. Confirmed cash checkout records sale, payment, inventory movement, and cash movement atomically; transfers never inflate physical cash.
9. Closing a cash session shows expected cash, counted cash, and any discrepancy without rewriting prior movements.
10. Consultations distinguish completed sales, pending/confirmed/rejected collections, reservations, stock movements, and cash sessions.
11. The internal receipt shows the confirmed sale and is clearly non-fiscal.
12. Strict TDD and observed verification evidence are recorded for implemented behavior; unavailable checks remain explicitly pending.

## Tasks
- [x] **POS-01 — Record domain glossary, decision, and Mermaid flows.** Work-unit commit: `e3fcaefd3f6692d6cb085d484d09aecfe6f83427` (`docs(pos): document domain and transfer flows`). Independent verification confirmed the scoped documentation; no test suite applies. The reviewer noted a minor reservation-conversion flow clarity follow-up for POS-06. Create the root `CONTEXT.md`, one ADR for transfer-verification timing, and `docs/product/mvp-flows.md` with user and system diagrams. Acceptance observed: terminology is consistent; both diagrams cover accepted transitions, one-store/register scope, and database-verified supervisor authorization.
- [x] **POS-02 — Establish the Bun/Turborepo web workspace.** Work-unit commit: `f35c3ba92f98889ccab13b8cfd355b43470d03eb` (`feat(pos): establish web workspace`); size exception accepted for its future PR slice. Independent verification confirmed the exact committed scope. `bun run test --force` passed (1/1), `bun run typecheck --force` passed uncached, `bun run build` passed on cache miss, and `git show --check` reported no whitespace issues. `bun install` had passed during implementation; a clean-environment frozen-lockfile install was not run.
- [x] **POS-03 — Establish Supabase identity and authorization.** Work-unit commit: `795edfa` (`feat(pos): establish Supabase authorization`); size exception accepted for its future PR slice. The 29/29 pgTAP run was independently observed during implementation; a separate exact-commit verifier confirmed the three-file POS-03 boundary and statically counted the 29 assertions. No DB tests were rerun by that commit verifier.
- [x] **POS-04 — Implement catalog and inventory database operations.** Work-unit commit: `f627404394c5140a38ab3a370b7c6cd56ee43061` (`feat(pos): add catalog inventory operations`); size exception accepted for its future PR slice. The exact-commit independent verifier passed `git show --check` and `bun run db:test` (82/82 across both suites), confirmed the three-path boundary and POS-03 parent, and found no blockers. Remaining non-blocking coverage is recorded for POS-10.
- [ ] **POS-05 — Build catalog and inventory screens.** Add product administration, stock receipt/adjustment forms, current availability, and movement history. Acceptance: cashier/admin permissions are reflected in UI while still enforced by Postgres.
- [ ] **POS-06 — Implement simultaneous pre-accounts and customer reservations.** Keep editable pre-accounts separate from expiring stock reservations. Acceptance: pre-accounts do not change stock; reservations affect availability and release stock on conversion, cancellation, or expiry.
- [ ] **POS-07 — Implement cash sessions.** Add opening float, confirmed cash movements, controlled adjustments, and closing reconciliation. Acceptance: transfers do not affect physical cash and closed sessions retain immutable movement history.
- [ ] **POS-08 — Implement checkout, payments, and transfer verification.** Add atomic cash checkout and a stock hold for manually verified transfers. Acceptance: only approved transfers produce confirmed sales; rejected transfers release stock; retries do not duplicate records.
- [ ] **POS-09 — Implement consultations and internal receipt.** Add separate views for sales, collections, reservations, inventory, and cash, plus browser-printable internal receipts. Acceptance: pending items are never presented as completed sales or cash.
- [ ] **POS-10 — Verify end-to-end invariants and security.** Exercise role boundaries, concurrency, lifecycle transitions, and the full application suite. Acceptance: all applicable checks have recorded outcomes and no unresolved high-impact invariant remains.

## Progress
- Requirements, DIAN scope, transfer stock-hold behavior, and strict TDD/Vitest selection confirmed by the user. POS-04 is intentionally split into database and UI slices to keep inventory invariants and review scope focused.
- Read-only checkout exploration found no application scaffold; existing untracked `odd/` artifacts will be preserved and not reused as requirements.
- ODD ledger created at `odd/tasks/mundo-kawaii-pos-mvp.md`; full Engram mirror saved and read back as observation `1462`.
- POS-01 was implemented by one bounded writer because it touched three non-trivial documentation files. Parent readback passed; an independent verifier found two omissions (one-store/register scope and supervisor authorization), which were corrected by a bounded writer and confirmed by a second independent verifier with no findings. The authorized work-unit commit is `e3fcaefd3f6692d6cb085d484d09aecfe6f83427` (`docs(pos): document domain and transfer flows`), created on `feat/planning`. A separate verifier confirmed the committed range and `git show --check`; no test suite applies. A minor question about the downstream transition of a converted reservation is deferred to POS-06.
- POS-02 was implemented by one bounded writer under the multi-file trigger. Strict TDD evidence: route assertion failed before implementation (RED), passed after implementation (GREEN), and passed after refactor. The authorized work-unit commit is `f35c3ba92f98889ccab13b8cfd355b43470d03eb` (`feat(pos): establish web workspace`), created after POS-01 on `feat/planning`. Independent verification inspected the exact committed diff and confirmed root/app scripts, lockfile workspace declarations, and absence of POS-03/04 implementation. `git show --check` passed; `bun run test --force` passed (1/1); `bun run build` passed with a cache miss; a first typecheck verifier run hit cache, then a separate `bun run typecheck --force` completed `tsc --noEmit` successfully. `bun install` passed during implementation; a clean-environment frozen-lockfile install was not run. The 601-line lockfile explains the explicitly accepted future PR size exception.
- POS-03 was implemented by one bounded writer under the multi-file trigger. Strict TDD: pgTAP was RED before the migration; after implementation/reset 28 tests passed; a refactor added protection against Auth-account deletion bypassing role audit, then 29/29 passed. `bun install`, `bun run db:start`, `bun run db:reset`, `bun run db:test`, `bun run test`, and `bun run typecheck` passed. Independent verification found no security blockers and reran `bun run db:test` (29/29); concurrent sessions are not covered yet and remain for POS-09. The authorized commit is `795edfa` (`feat(pos): establish Supabase authorization`), created after POS-02 on `feat/planning`. A separate exact-commit verifier confirmed the three-file boundary, `git show --check`, and a static count of 29 assertions; it did not rerun DB tests because the aggregate worktree contains untracked POS-04 files.
- POS-04 resume reconciliation: the in-repo ledger and Engram observation `1462` agreed on scope and strict-TDD runners. A read-only mapper identified authorization, pgTAP, and local-database conventions. The user explicitly confirmed administrator-only catalog changes, receipts, and adjustments, with cashiers read-only. One bounded direct writer prepared `CONTEXT.md`, `supabase/README.md`, `supabase/tests/catalog_inventory.test.sql`, and `supabase/migrations/20260925113124_catalog_inventory.sql` under the multi-file trigger. `CONTEXT.md` was included in the earlier POS-01 domain-docs commit; the remaining POS-04 commit boundary is the shared `supabase/README.md`, the catalog/inventory migration, and its pgTAP test. Strict TDD evidence reported by the writer: the test-only run was RED on the missing schema/RPC before the migration; after `bun run db:reset`, `bun run db:test` passed 82/82 across two files. An independent verifier also reran `bun run db:test` and observed 82/82 passing; parent readback confirmed row-locked receipt/adjustment RPCs and no client table-write grants. Native ASSESS returned `unassessable` because the worktree contains undeclared untracked files; its RDD-off plan required an independent verifier, which ran. No native review was started. Remaining coverage notes: adjustment movement-insert rollback is not separately fault-injected, and simultaneous sessions are not exercised here; POS-10 retains concurrency verification. The verifier also noted that a privileged database owner can TRUNCATE the ledger, while application roles have no table-write/TRUNCATE privilege and UPDATE/DELETE are rejected. The user explicitly authorized four ordered POS-01–POS-04 work-unit commits and selected `stacked-to-main` for future PRs. A read-only commit-boundary audit found that POS-04 depends on POS-03's authorization migration and the accumulated work exceeds the 400-line budget. POS-01 through POS-04 were subsequently committed and independently verified; the POS-04 commit is `f627404394c5140a38ab3a370b7c6cd56ee43061`. No push, PR, or merge is authorized.
- POS-03/POS-04 path mapping classified `supabase/config.toml`, `supabase/migrations/20260617000000_initial_authorization.sql`, and `supabase/tests/roles.test.sql` as POS-03; the catalog migration and 53-assertion test as POS-04; and `supabase/README.md` as shared prose to defer until POS-04. The inventory migration calls authorization helpers from POS-03, so commit order is required. The role test declares 29 assertions, matching its prior 29/29 result; the two test plans sum to the later aggregate 82/82.
- One-pass review-slice analysis found cohesive sub-slices for POS-03 and POS-04 under 400 lines, but POS-02's 601-line `bun.lock` cannot be separated from app dependencies. The user chose to keep one work-unit commit per POS-01–POS-04 and explicitly accepted `size:exception` for POS-02/03/04 future PRs. Commit order: POS-01 docs, POS-02 web workspace, POS-03 authorization, POS-04 catalog/inventory. All four commits are complete, with each task's paths kept within its boundary.
- Native risk assessments for untracked docs/app/SQL files were unassessable because the native tool requires an explicit intended-untracked declaration; RDD is off. Followed each returned high-risk plan with independent verification; no native review was started.
- TDD source: explicit user choice in this session. TypeScript runner: Vitest via root `bun run test`, established and verified in POS-02. Database runner: pgTAP via Supabase CLI `supabase test db`, confirmed by official Supabase documentation.
- Delivery strategy: `ask-on-risk`; the user selected `stacked-to-main` and explicitly accepted size exceptions for the POS-02/03/04 PR slices. POS-02's `bun.lock` alone is 601 lines; POS-03 and POS-04 were estimated above 400 lines before the one-pass sub-slice proposal. Create exactly the four authorized task commits on `feat/planning`; do not push or create PRs yet.

## Next step
The four authorized work-unit commits are complete and independently verified: POS-01 `e3fcaefd3f6692d6cb085d484d09aecfe6f83427`, POS-02 `f35c3ba92f98889ccab13b8cfd355b43470d03eb`, POS-03 `795edfa122ca32e20c87142fb59766dbe2981300`, and POS-04 `f627404394c5140a38ab3a370b7c6cd56ee43061`. Await user direction before starting POS-05 UI implementation. The user accepted size exceptions for POS-02/03/04 future stacked-to-main PR slices; no push, PR, or merge is authorized. POS-10 still needs concurrent inventory-writer tests.
